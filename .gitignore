# dependencies
node_modules
.pnp
.pnp.js

# next
.next
out
next-env.d.ts

# production
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env
.env.development
.env.production

# vercel
.vercel

# turbo
.turbo

# coverage
coverage/

# typescript
*.tsbuildinfo

# design package source (keep tracked? — for now ignore the zip/tar)
_design_package/*.zip
_design_package/design.tar

# editor
.vscode/
.idea/

# supabase CLI cache (per-machine, contains linked-project.json, pooler-url, etc.)
supabase/.temp/
supabase/.branches/

# proyecto original ReseñaHub — repo git independiente anidado, NO debe rastrearse aquí
# (sirve solo como fuente de referencia para portar features). Tiene su propio .git/node_modules/.next.
00 - Proyecto Original./

# artefactos locales que NO deben acabar en el repo (es PÚBLICO):
# - graphify-out/: mapa completo de la arquitectura del código (graph.json, resúmenes LLM)
# - Venta/: material de negocio (buyer personas, etc.)
# - .bd.txt / .tmpbuild/: notas y builds temporales
graphify-out/
Venta/
.bd.txt
.tmpbuild/
.env*

# Documentos internos de negocio (no publicar: repo público)
# (incluye Plan de contenidos/Buyer Persona/ — el Excel de buyer personas vive ahí dentro)
Plan de contenidos/
